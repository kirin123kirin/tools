# CLAUDE.md

このリポジトリで作業する際のガイドライン。

## プロジェクト概要

Windows 11 専用。さまざまな処理を `tools <name>` サブコマンドとして追加していくPythonプロジェクト。
構成・追加方法は `README.md` を参照。

設計ドキュメント類（仕様書、設計メモなど）は `doc/` フォルダに置くこと。

## バージョン管理（コミットごとに自動でパッチバージョンを上げる）

このプロジェクトはコミットのたびにバージョンをインクリメントする運用とする。
Claude Codeがコミットを作成する際は、**同じコミットの中で**以下を行うこと。

1. `pyproject.toml` の `version` の**パッチ番号を+1**する
   （例: `0.1.0` → `0.1.1`。MINOR/MAJORの繰り上げは、破壊的変更や
   まとまった機能追加など、人間が明示的に指示した場合のみ行う）
2. `CHANGELOG.md` の `[Unreleased]` セクションに変更内容を追記し、
   新しいバージョン番号の見出しへ切り出す
3. 通常通りコミットを作成する
4. コミット後、`git tag -a vX.Y.Z -m "..."` で**そのバージョンに対応する
   タグを同時に作成する**（`pyproject.toml`のversionとgit tagが常に
   一致した状態を保つ）
5. ユーザーからpushの許可を得ている場合は、コミットと合わせて
   タグもpushする（`git push && git push origin vX.Y.Z`）

軽微なドキュメント修正やテストのみの変更であっても、コミットする限りは
このバージョンアップを省略しないこと。

## 社内セキュリティ製品（DLP/EDR）との共存を前提とした設計

社内で配布・利用することを前提に、DLP/EDRなどの監視製品から**誤検知（false positive）されにくい、
挙動が透明なツール**として作る。「検出を回避する」ことを目的にコードを難読化したり挙動を隠したりは
しない。方針は以下の通り。

- **標準・公開APIのみを使う**: クリップボード取得はPillowの`ImageGrab`など公式に文書化されたAPI経由に
  限定する。生のWindowsフック（`SetWindowsHookEx`等によるグローバルフック）やメモリ改ざん、
  他プロセスへのインジェクションは行わない。
- **難読化・パッキングをしない**: 配布形態がexe化される場合も、コード内容がそのまま追える状態を保つ
  （PyInstallerの単一exeでの圧縮・暗号化オプションや、意図的な難読化ツールは使わない）。
- **永続化機構を勝手に仕込まない**: レジストリRunキー、スケジュールタスク、サービス登録などの
  永続化は、明示的に依頼された場合を除き実装しない。
- **通信先を明示する**: 外部通信が発生する処理（例: `touka`が初回に行うモデルダウンロード）は、
  接続先ホストをREADME等に明記し、IT部門がプロキシ/EDRのアローリストに登録できるようにする。
- **常駐・バックグラウンド監視をしない**: クリップボード監視などは、ユーザーがコマンドを実行した
  その場でのみ1回読み取る。裏で常駐して監視し続けるような実装（キーロガー的挙動と誤認されやすい）
  は避ける。
- **ログを明確に出す**: 何をしたか（読み込み元・出力先など）を`common/logging.py`経由で分かる形に
  ログ出力し、動作が不透明にならないようにする。
- 上記の制約により既存のセキュリティ製品で誤検知が発生する場合は、挙動を隠す方向で対応するのではなく、
  IT部門への申請・アローリスト登録で解決することを前提とする。
